{
  "name": "electron-sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package-linux": "electron-packager . --overwrite --platform=linux --arch=x64 --prune=true --ignore='.git' --out=builds",
    "package-win": "electron-packager . --overwrite --asar=true --platform=win32 --arch=ia32 --prune=true --ignore='.git' --out=builds"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/antirek/electron-sample.git"
  },
  "author": "Sergey Dmitriev <antirek@yandex.ru>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/antirek/electron-sample/issues"
  },
  "homepage": "https://github.com/antirek/electron-sample#readme",
  "devDependencies": {
    "electron": "^1.7.5"
  },
  "dependencies": {
    "express": "^4.15.4",
    "pug": "^2.0.0-rc.3"
  }
}
